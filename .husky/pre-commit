#!/usr/bin/env sh
# https://typicode.github.io/husky/
. "$(dirname -- "$0")/_/husky.sh"

rm -f yarn.lock
rm -f yarn-error.log
rm -f package-lock.json

yarn build

echo '🟢 Build Front Finalizado'


